spring.replica.datasource.driver-class-name=org.postgresql.Driver
spring.replica.datasource.url=${CITIZEN_REPLICA_CONN_URL:${POSTGRES_CONN_URL:jdbc:postgresql://${CITIZEN_REPLICA_DB_HOST:${POSTGRES_HOST:localhost}}:${CITIZEN_REPLICA_DB_PORT:${POSTGRES_PORT:5432}}/${CITIZEN_REPLICA_DB_NAME:${POSTGRES_DB_NAME:}}}}
spring.replica.datasource.username=${CITIZEN_REPLICA_DB_USERNAME:${POSTGRES_USERNAME:}}
spring.replica.datasource.password=${CITIZEN_REPLICA_DB_PASSWORD:${POSTGRES_PASSWORD:}}
spring.replica.datasource.hikari.maximumPoolSize=${POSTGRES_REPLICA_POOLSIZE:${POSTGRES_POOLSIZE:5}}
spring.replica.datasource.hikari.connectionTimeout=${POSTGRES_TIMEOUT:6000}
spring.replica.datasource.hikari.schema=${CITIZEN_DB_SCHEMA:${POSTGRES_SCHEMA:}}
spring.replica.datasource.hikari.readOnly=true
spring.replica.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.replica.jpa.hibernate.ddl-auto=none
spring.replica.jpa.show-sql=${POSTGRES_SHOW_SQL:true}
spring.replica.jpa.open-in-view=false
spring.replica.jpa.database-platform=org.hibernate.dialect.PostgreSQL95Dialect